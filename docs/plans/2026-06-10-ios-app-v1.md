# Plan: MiraNote iOS app v1 frontend

**REQUIRED SUB-SKILL:** run-loop (loop contract) + verify-repo (verification)
**Refs:** #2 | spec: docs/specs/2026-06-10-ios-app-v1-design.md

## Loop contract

**Goal -- acceptance criteria:**

- [ ] AC1: Repo has an Xcode project that builds for iOS Simulator via
      `xcodebuild build` with zero errors.
- [ ] AC2: Unit tests exist and pass via `xcodebuild test` on a simulator
      destination; view models for Home/Canvas/sheets are covered.
- [ ] AC3: SwiftLint runs clean (`swiftlint --strict` on changed files).
- [ ] AC4: All four screens from the spec exist and navigate as drawn:
      Home -> Canvas -> Text sheet / AI Sticker sheet / Style Transfer sheet.
- [ ] AC5: D1-D3 decisions are implemented (3-image cap constant, separate
      sticker/style entries, empty-state hint on Home).
- [ ] AC6: Governance checks green (run per verify-repo from a clean
      origin/main checkout of MiraNote-AI/.github).
- [ ] HUMAN: visual fidelity to Jason's sketches and interaction feel --
      Jason reviews on device/simulator.
- [ ] HUMAN: Q1-Q4 in the spec answered by Jason.

**Stop conditions:** iteration cap 5 per task below; 2 consecutive
no-progress iterations -> handoff; escalation if protected paths, weakened
checks, or scope beyond the referenced issue would be needed.

**Budget:** n/a (interactive session loop, not scheduled).

## Tasks

- [x] T0: Design spec + this plan + .gitignore (PR: docs/ios-v1-spec)
- [ ] T1 (BLOCKED on Q1/Jason + Xcode install): project scaffold -- either
      rebase Jason's prototype or fresh XcodeGen scaffold; includes SwiftLint
      config and test target. PR closes nothing; Refs the issue.
- [ ] T2: Home screen (empty state per D3, collections row, both pills)
- [ ] T3: Canvas screen (top bar, long-press menu, bottom toolbar + drawer)
- [ ] T4: Text input sheet (blur backdrop, voice/clean/expand/polish row
      wired to mock service)
- [ ] T5: AI Sticker sheet + Style Transfer sheet (D1 3-image cap, D2 split)
- [ ] T6: integration pass -- navigation, mock service layer, full AC sweep

## Iterations

```
0. 2026-06-10 contract written; T0 in flight -- criteria 0/6 passing
```

## Deviations

(none yet)
